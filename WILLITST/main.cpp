#include <bits/stdc++.h>
typedef long long       ll;

using namespace std;
int main() {
    ll n;
    cin >> n;
    ceil(log2(n)) == floor(log2(n)) ? cout << "TAK" << endl : cout << "NIE" << endl;
    return 0;
}